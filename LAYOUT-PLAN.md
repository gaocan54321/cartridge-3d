# 布局规划文档 — Cartridge Portfolio

> **设计核心**：「我是一台游戏机，每盘卡带是我的一段经历 / 一个作品」。
> 3D 卡带不是装饰，而是**全站主导航**——悬停预览、点击"插入"进入内容。

---

## 一、设计理念：卡带即导航

| 层级 | 作用 | 实现方式 |
|---|---|---|
| **卡带层** | 导航入口 + 视觉灵魂 | Three.js 3D 场景，6 盘卡带 = 6 个内容入口 |
| **内容层** | 信息展示 | 2D 页面区块，滚动或点击卡带后展开 |
| **转场层** | 沉浸感连接 | "插入卡带"动画 + CRT 闪烁，把 3D 世界和 2D 内容缝合成一个体验 |

**关键原则**：
- 卡带是**首屏唯一主角**，所有个人介绍内容通过"插入卡带"动作逐步展开。
- 不抢戏：3D 动画永远服务于内容，而非阻碍内容。
- 招聘方导向：首屏 3 秒内看到定位语 + 简历下载按钮。

---

## 二、响应式断点策略

```
Desktop  (>1024px)  → 完整 3D，6 卡带 2×3 阵列，全交互
Tablet   (768–1024px) → 3D 保留，相机拉远，卡带间距压缩，hover → tap
Mobile   (<768px)     → 3D 降级为静态渲染 + 垂直卡带列表（性能优先）
```

### 各端布局对照

| 元素 | Desktop | Tablet | Mobile |
|---|---|---|---|
| 3D 场景 | 全屏背景，6 卡带悬浮 | 全屏背景，相机拉远 | 首屏静态渲染图或简版 3D（单卡带轮播） |
| 卡带阵列 | 2 行 × 3 列 | 2 行 × 3 列（间距缩小） | 垂直列表，单张卡片式展示 |
| 悬停效果 | Raycaster + 阻尼动画 | 同 Desktop | 无悬停，点击展开 |
| 相机视差 | 鼠标跟随 | 减弱 | 关闭（陀螺仪可选） |
| 内容入口 | 点击卡带"插入" | 点击卡带"插入" | 点击卡片展开详情 |
| 导航辅助 | 无（卡带即导航） | 无 | 底部固定 Tab Bar |

---

## 三、6 盘卡带的内容映射

每张卡带对应一个内容区块，标签贴图需重新设计为**个人内容主题**。

| # | 卡带外壳色 | 标签主题 | 对应内容区块 | 标签图案方向 |
|---|---|---|---|---|
| 1 | 白色 `#eef0f3` | **About** 关于我 | 个人简介、定位语、技能标签 | 简洁留白 + 头像/名字 |
| 2 | 纯黑 `#17171b` | **Project A** 代表作 1 | 精选项目 1 详情 | 项目主题色 + 图标 |
| 3 | 浅蓝 `#a9c3e6` | **Project B** 代表作 2 | 精选项目 2 详情 | 项目主题色 + 图标 |
| 4 | 米黄 `#d9c9a1` | **Project C** 代表作 3 | 精选项目 3 详情 | 项目主题色 + 图标 |
| 5 | 灰褐 `#cfc9b8` | **Find Me** 社交链接 | GitHub / 博客 / 抖音 / 小红书入口 | 社交图标拼贴 |
| 6 | 深藏青 `#31405e` | **???** 彩蛋 | Coming soon / 暗示未来经历 | 像素问号 + 神秘感 |

> **贴图设计原则**：保留现有的 Canvas 2D 程序化绘制风格，但将品牌 Logo 替换为个人内容标识（名字缩写、项目图标、技能标签等）。双语版本需准备两套贴图（中文/英文）。

---

## 四、页面区块规划（自上而下）

### 区块 0：首屏 Hero（3D 卡带场景）

```
┌─────────────────────────────────────────────┐
│  [EN/中]                    [↓ Download PDF] │  ← 顶部工具栏
│                                             │
│          ┌─────────────────────┐            │
│          │  前端工程师 · 5 年   │            │  ← 一句话定位
│          │  专注 XX 领域        │            │
│          └─────────────────────┘            │
│                                             │
│    [卡带1]  [卡带2]  [卡带3]                │  ← 3D 卡带阵列
│                                             │
│    [卡带4]  [卡带5]  [卡带6]                │
│                                             │
│         把鼠标悬停在卡带上 · Hover           │  ← 提示文案
│                                             │
└─────────────────────────────────────────────┘
```

**首屏硬约束（招聘方导向）**：
1. 3 秒内看到：**定位语** + **下载简历按钮** + **联系方式**
2. 下载按钮常驻右上角，任何滚动位置可达
3. 联系方式（邮箱 / GitHub）在首屏角落露出，不藏在页尾
4. 所有动画可跳过（点击立即进入内容）

### 区块 1：About 卡带内容（点击"插入 About 卡带"后展开）

```
┌─────────────────────────────────────────────┐
│  🎮 INSERT COIN → About                      │  ← 区块标题带像素风前缀
│                                             │
│  ┌──────────┐  你好，我是 [名字]。            │
│  │ 头像/头像 │  一句话简介（3 行以内）。       │
│  └──────────┘                                │
│                                             │
│  技能标签：[React] [TypeScript] [Three.js] ... │
│                                             │
│  邮箱：xxx@email.com  GitHub：@username      │
└─────────────────────────────────────────────┘
```

### 区块 2：工作经历时间线（招聘方最关心，放最前）

```
┌─────────────────────────────────────────────┐
│  💼 WORK EXPERIENCE                          │
│                                             │
│  ┌──────┐                                   │
│  │ 2024 │  公司 A · 高级前端工程师           │
│  ├──────┤  一句话描述职责 + 成果数据          │
│  │ 2022 │                                   │
│  ├──────┤  公司 B · 前端工程师               │
│  │ 2020 │  一句话描述职责 + 成果数据          │
│  └──────┘                                   │
│                                             │
└─────────────────────────────────────────────┘
```

**设计方向**：时间线左侧用像素风节点（圆点或卡带插槽图标），右侧为文字内容。保持与红白机主题的一致性。

### 区块 3：精选项目（3 盘卡带对应的内容）

每个项目以**"卡带展开"**的形式展示：

```
┌─────────────────────────────────────────────┐
│  🎯 FEATURED PROJECTS                        │
│                                             │
│  ┌───────────────────────────────────────┐  │
│  │ [卡带 2 贴图预览]  项目名称            │  │
│  │                    技术栈标签           │  │
│  │  [Live Demo] [GitHub]                  │  │
│  └───────────────────────────────────────┘  │
│  ┌───────────────────────────────────────┐  │
│  │ [卡带 3 贴图预览]  项目名称            │  │
│  │                    技术栈标签           │  │
│  │  [Live Demo] [GitHub]                  │  │
│  └───────────────────────────────────────┘  │
│  ┌───────────────────────────────────────┐  │
│  │ [卡带 4 贴图预览]  项目名称            │  │
│  │                    技术栈标签           │  │
│  │  [Live Demo] [GitHub]                  │  │
│  └───────────────────────────────────────┘  │
│                                             │
└─────────────────────────────────────────────┘
```

### 区块 4：全部项目网格

```
┌─────────────────────────────────────────────┐
│  📂 ALL PROJECTS                             │
│                                             │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐           │
│  │项目1│ │项目2│ │项目3│ │项目4│  ...      │  ← 2D 卡片网格
│  └─────┘ └─────┘ └─────┘ └─────┘           │
│                                             │
└─────────────────────────────────────────────┘
```

> 项目总数 > 6 时采用「首页 3D 精选 + 完整 2D 网格」方案，保证 3D 场景永远只渲染 6 盘，性能可控。

### 区块 5：Find Me 社交链接（对应卡带 5）

```
┌─────────────────────────────────────────────┐
│  🌐 FIND ME                                  │
│                                             │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐       │
│  │  GitHub │ │  博客   │ │  抖音   │       │
│  │ 图标    │ │ 图标    │ │ 图标    │       │
│  └─────────┘ └─────────┘ └─────────┘       │
│                                             │
│  邮箱：xxx@email.com                        │
└─────────────────────────────────────────────┘
```

> 设计为"插入社交卡带"后的展开效果，而非简单的图标排列，保持叙事一致性。

### 区块 6：页脚

```
┌─────────────────────────────────────────────┐
│                                             │
│  © 2025 INSERT COIN · 名字                  │
│  用 ❤️ 和像素构建                            │
│                                             │
└─────────────────────────────────────────────┘
```

---

## 五、交互流程设计

### 主流程：首屏 → 插入卡带 → 内容展开

```
┌─────────────┐     hover      ┌─────────────┐     click      ┌─────────────┐
│  待机状态   │ ─────────────→ │  悬停状态   │ ─────────────→ │  插入动画   │
│  卡带浮动   │   翘起/上浮    │  卡带翘起   │   卡带下沉     │  卡带推向   │
│  轻微摇摆   │   微放大       │  标签高亮   │   镜头拉近     │  屏幕深处   │
└─────────────┘                └─────────────┘                └──────┬──────┘
                                                                     │
                                                                     ▼
                                                              ┌─────────────┐
                                                              │  CRT 闪烁   │
                                                              │  转场效果   │
                                                              └──────┬──────┘
                                                                     │
                                                                     ▼
                                                              ┌─────────────┐
                                                              │  内容区展开 │
                                                              │  对应卡带   │
                                                              │  内容展示   │
                                                              └─────────────┘
```

### 滚动行为

| 场景 | 行为 |
|---|---|
| 首屏向下滚动 | 3D 场景可保持固定（视差背景），或随滚动淡出，内容区上滑覆盖 |
| 内容区内滚动 | 正常页面滚动，3D 场景在背景中以较低透明度/模糊度存在 |
| 返回顶部 | 点击"拔出卡带"按钮，回到首屏 3D 场景 |

### 卡带点击后的两种展开模式

**模式 A：单页滚动（推荐，更符合现代作品集习惯）**
- 点击卡带后，页面平滑滚动到对应内容区块
- 3D 卡带执行"插入"动画（下沉 + 推向深处 + CRT 闪烁）
- 滚动到对应区块时，区块标题旁显示该卡带的 2D 缩略图

**模式 B：独立页面（更具游戏感）**
- 点击卡带后，全屏 CRT 转场
- 跳转到独立的内容页面（如 `/about`, `/project/xxx`）
- 页面顶部有"⏏ Eject"按钮返回首页

> **建议**：采用模式 A（单页滚动），招聘方更熟悉，且 SEO/性能更好。模式 B 可作为彩蛋变体。

---

## 六、移动端降级方案

### 为什么必须降级？

- **性能**：移动端 GPU 性能有限，6 个带阴影的 3D 模型 + Raycaster 每帧检测 = 掉帧/发热
- **交互**：触摸没有 hover 状态，Raycaster 的悬停逻辑需要改为 tap
- **视口**：手机屏幕窄，6 卡带 2×3 阵列会挤在一起，可读性差

### 降级策略

```
Mobile (< 768px)
│
├─ 首屏：静态卡带渲染图（可预渲染 1 张高质量 PNG）
│   或简版 3D：单卡带轮播，左右滑动切换
│
├─ 导航：底部固定 Tab Bar（5 个入口）
│   [首页] [About] [项目] [联系] [简历↓]
│
├─ 内容：垂直卡片列表，每张卡片对应一盘卡带
│   卡片左侧为卡带缩略图，右侧为标题 + 简介
│
└─ 交互：点击卡片展开详情（手风琴或底部 Sheet）
```

### 移动端首屏草图

```
┌─────────────────┐
│ [中/EN]   [↓CV] │  ← 顶部工具栏
│                 │
│  ┌───────────┐  │
│  │           │  │  ← 卡带轮播（单张，可滑动）
│  │  卡带大图  │  │
│  │           │  │
│  └───────────┘  │
│                 │
│  ● ○ ○ ○ ○ ○   │  ← 轮播指示器
│                 │
│  前端工程师     │  ← 定位语
│  5 年 · 专注 XX │
│                 │
│  [查看详情 →]   │
│                 │
├─────────────────┤
│ 🏠  👤  💼  🌐  ⬇│  ← 底部 Tab Bar
└─────────────────┘
```

---

## 七、视觉风格统一方案

### 色彩体系

```
主背景：#fbfcfe → #eef1f5 → #e3e8ee（径向渐变，已存在）
文字主色：slate-700 / #334155
文字次要：slate-400 / #94a3b8
强调色：#31405e（深藏青，来自 Tuenti 卡带）
像素点缀：#c0392b（复古红，来自 Azure 卡带条纹）
```

### 字体策略

| 用途 | 字体 | 说明 |
|---|---|---|
| 大标题 | `Press Start 2P` 或像素风字体 | 仅用于"INSERT COIN"、区块标题前缀 |
| 正文 | `Inter` / system-ui | 现代无衬线，保证可读性 |
| 代码/标签 | `JetBrains Mono` | 技术标签、日期 |
| 装饰 | `Georgia` italic | 可选，用于引用或特殊文案 |

### 像素风使用边界

- ✅ 区块标题前缀（如 🎮、💼、🎯）
- ✅ 按钮 hover 状态的 8-bit 音效（可选）
- ✅ 页脚彩蛋文案（"INSERT COIN"）
- ✅ 加载动画的 CRT 扫描线
- ❌ 正文段落（可读性差）
- ❌ 导航菜单（易读性优先）

### 组件风格

- 卡片：圆角（与卡带的 `RoundedBoxGeometry` 呼应），轻微阴影
- 按钮：主按钮用深藏青 `#31405e`，hover 时有像素风边框动画
- 标签：小型圆角标签，与卡带贴纸风格一致

---

## 八、实现优先级（迭代路线）

### Phase 1：内容基础设施（必须先有真实内容）
- [ ] 收集/编写：个人定位语、工作经历、3 个精选项目、全部项目清单
- [ ] 准备照片/头像（或像素风头像）
- [ ] 设计 6 张卡带的**新标签贴图**（将品牌 Logo 替换为个人内容）

### Phase 2：首屏完善
- [ ] 添加顶部工具栏（语言切换 + 下载简历）
- [ ] 添加一句话定位语到首屏叠加层
- [ ] 实现卡带点击 → 滚动到对应区块 的联动

### Phase 3：内容区块
- [ ] About 个人简介区块
- [ ] 工作经历时间线区块
- [ ] 精选项目 ×3 区块
- [ ] 全部项目 2D 网格
- [ ] Find Me 社交区块
- [ ] 页脚

### Phase 4：动画与转场
- [ ] "插入卡带"动画（卡带下沉 + 推向深处 + 镜头拉近）
- [ ] CRT 开机/转场闪烁效果
- [ ] 区块进入视口时的滚动动画

### Phase 5：响应式与移动端
- [ ] 移动端 3D 降级为静态渲染/轮播
- [ ] 底部 Tab Bar 导航
- [ ] 触摸友好的卡片列表
- [ ] 性能优化（减少移动端 3D 渲染负担）

### Phase 6：国际化
- [ ] 文案抽离为字典（i18next 或轻量 Context）
- [ ] 双语卡带贴图机制（根据语言切换 CanvasTexture）

### Phase 7：部署
- [ ] Vercel 部署
- [ ] （可选）自定义域名
- [ ] SEO 优化（meta 标签、Open Graph）

---

## 九、文件结构规划

```
src/
├── pages/
│   └── Home.tsx              # 主页面，组合所有区块
├── sections/
│   ├── HeroSection.tsx       # 首屏 3D 场景 + 叠加层
│   ├── AboutSection.tsx      # 关于我
│   ├── ExperienceSection.tsx # 工作经历时间线
│   ├── FeaturedProjects.tsx  # 精选项目（3 个）
│   ├── AllProjects.tsx       # 全部项目网格
│   ├── FindMeSection.tsx     # 社交链接
│   └── Footer.tsx            # 页脚
├── components/
│   ├── CartridgeGallery.tsx  # 3D 场景容器（现有）
│   ├── CartridgeCard.tsx     # 2D 卡带卡片（移动端/项目列表用）
│   ├── Timeline.tsx          # 时间线组件
│   ├── ProjectCard.tsx       # 项目卡片
│   ├── TabBar.tsx            # 移动端底部导航
│   ├── LanguageSwitch.tsx    # 语言切换
│   ├── ResumeButton.tsx      # 下载简历按钮
│   └── ui/                   # shadcn/ui 组件
├── three/
│   ├── createCartridge.ts    # 程序化建模（现有）
│   ├── labels.ts             # 贴图绘制（现有，需扩展双语）
│   └── scene.ts              # 场景初始化（从 CartridgeGallery 抽取）
├── hooks/
│   ├── use-mobile.ts         # 移动端检测（现有）
│   └── use-scroll-to.ts      # 平滑滚动到指定区块
├── lib/
│   ├── utils.ts              # 工具函数（现有）
│   └── i18n.ts               # 国际化配置
└── data/
    ├── profile.ts            # 个人资料数据
    ├── experience.ts         # 工作经历数据
    └── projects.ts           # 项目数据
```

---

## 十、关键决策记录

| 决策 | 选择 | 理由 |
|---|---|---|
| 单页 vs 多页 | **单页滚动** | 招聘方浏览习惯，SEO 友好，3D 场景可保持背景 |
| 移动端 3D | **降级为静态/轮播** | 性能优先，触摸交互不匹配 hover |
| 卡带点击行为 | **滚动到对应区块** | 直观、快速、不打破页面流 |
| 像素字体使用 | **仅标题点缀** | 正文可读性优先 |
| 6 卡带限制 | **保持 6 盘** | 性能可控，叙事完整（精选 + 社交 + 彩蛋） |
| 语言切换 | **Context + 两套贴图** | 轻量，贴图需 Canvas 重绘 |

---

> 📎 相关文档：
> - `PROJECT.md` — 项目定位、受众、硬约束
> - `TECH-STACK.md` — 技术栈详情、给其他 AI 的 Prompt
