# 技术栈文档

> 一句话概括：**Three.js 程序化建模 + Canvas 程序化贴图 + 射线拾取悬停动画，零外部素材。**

---

## 给其他 AI 模型的自然语言描述（可直接复制）

用 Three.js 帮我做一个网页：浅色渐变背景上悬浮着 6 张红白机（Famicom）风格的 3D 游戏卡带，排成 2 行 3 列。

- **建模要求**：不要用外部模型文件，用 `RoundedBoxGeometry` 等基础几何体程序化拼出卡带——主壳体、顶部凸棱、底部向前伸出的卡带脚、正面凹陷的贴纸区域。6 张卡带外壳颜色分别为：白、纯黑、浅蓝、米黄、灰褐、深藏青。
- **贴图要求**：不要用图片素材，用 Canvas 2D 绘制后转 `CanvasTexture`，分别画出：空白标签、Vercel 黑底彩虹三棱镜、GitHub 深色底绿色贡献点阵图、Azure DevOps 复古像素风蓝底标签、Microsoft 名片式白底标签、Tuenti 蓝底标签。
- **交互要求**：用 `Raycaster` 做鼠标悬停检测，悬停的卡带平滑地向前翘起（绕 X 轴旋转约 -0.35 rad）、向镜头方向移动并微微放大，离开悬停时弹回，所有过渡用阻尼插值（damp/lerp）实现；待机状态下每张卡带带相位差的轻微上下浮动和左右摇摆；鼠标移动时相机有轻微视差跟随。
- **光影要求**：半球光 + 带 PCFSoft 软阴影的方向光，地面用 `ShadowMaterial` 只承接阴影，`ACESFilmicToneMapping`，整体明亮柔和的展示台质感。
- **技术栈不限**，单个 HTML 文件引入 Three.js CDN 即可。

> 把这段发给任何模型基本都能复现。关键点是说清三件事：**程序化建模**（不用模型文件）、**Canvas 生成贴图**（不用图片）、**射线拾取 + 阻尼动画的悬停效果**——这三个是原效果的灵魂，不说的话模型容易给你换成加载 GLTF 模型或贴图 URL 的方案。

---

## 技术栈分层

| 层 | 用的技术 | 作用 |
|---|---|---|
| 框架 | React 18 + TypeScript + Vite | 页面骨架和组件化（其实核心与框架无关，换成纯 HTML 也行） |
| 3D 引擎 | **Three.js**（核心） | 场景、相机、灯光、渲染全部靠它 |
| 3D 模型 | **程序化建模**：`RoundedBoxGeometry`（圆角盒体）+ 圆柱等基础几何体拼出卡带外壳 | 不依赖任何外部 3D 模型文件 |
| 卡带贴纸 | **Canvas 2D API 绘制 → `CanvasTexture`** | 用 JS 在画布上画出 Vercel/GitHub 等图案，再贴到 3D 面上 |
| 悬停交互 | **Raycaster（射线拾取）** 检测鼠标指向哪个 3D 物体 | 触发悬停状态 |
| 动画 | `THREE.MathUtils.damp`（阻尼插值）+ `requestAnimationFrame` | 悬停翘起/上浮/放大、待机浮动、镜头视差的平滑过渡 |
| 光影 | 半球光 + 方向光（PCF 软阴影）+ `ShadowMaterial` 地面阴影 + ACES 色调映射 | 柔和写实的质感 |
| UI 覆盖层 | Tailwind CSS | 标题、提示文字等 2D 部分 |
